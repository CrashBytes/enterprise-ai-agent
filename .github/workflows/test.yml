name: Tests

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11']
    
    steps:
    - uses: actions/checkout@v7
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Cache pip packages
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt', '**/requirements-dev.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi
        pip install pytest pytest-cov pytest-asyncio
    
    - name: Run tests with pytest
      run: |
        if [ -d tests ]; then
          pytest tests/ -v --cov=. --cov-report=xml --cov-report=term-missing
        elif [ -d test ]; then
          pytest test/ -v --cov=. --cov-report=xml --cov-report=term-missing  
        else
          echo "No tests directory found, running pytest discovery"
          rc=0
          pytest -v --cov=. --cov-report=xml --cov-report=term-missing || rc=$?
          if [ "$rc" -eq 5 ]; then
            echo "No tests collected (pytest exit 5) — scaffold has no tests yet, treating as success"
          elif [ "$rc" -ne 0 ]; then
            exit "$rc"
          fi
        fi
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false
